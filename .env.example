# Frontbase Environment Variables

# =============================================================================
# OPTIONAL - SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3000)
# PORT=3000

# Node environment (development, production)
# NODE_ENV=development

# Database path (default: ./database.sqlite)
# DB_PATH=./database.sqlite

# =============================================================================
# OPTIONAL - ADMIN USER CONFIGURATION
# =============================================================================

# Custom admin credentials (if not set, demo credentials will be used)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your_secure_password
# ADMIN_EMAIL=admin@yourdomain.com

# =============================================================================
# OPTIONAL - SESSION CONFIGURATION
# =============================================================================

# Session token expiry in milliseconds (default: 7 days)
# SESSION_EXPIRY=604800000

# =============================================================================
# OPTIONAL - REDIS CONFIGURATION (for session persistence & caching)
# =============================================================================

# Redis URL (default: redis://localhost:6379/0)
# REDIS_URL=redis://localhost:6379/0
# 
# Note: Redis is OPTIONAL for local development. If Redis is not running,
# the application will gracefully degrade - session data won't persist between
# page refreshes, but all core functionality will work.
# Redis is recommended for production deployments.

# =============================================================================
# PRODUCTION SECURITY NOTES
# =============================================================================

# For production deployments:
# 1. ALWAYS set custom ADMIN_USERNAME and ADMIN_PASSWORD
# 2. Use strong passwords (minimum 12 characters)
# 3. Set NODE_ENV=production
# 4. Use HTTPS in production
# 5. Consider setting up proper backup for your database