# Backend Redis — plain Redis instance for the control plane.
# Edge engines have their own embedded Redis.
FROM redis:7-alpine

# Persistence + memory limits for backend caching
CMD ["redis-server", "--save", "60 1", "--appendonly", "yes", "--maxmemory", "256mb", "--maxmemory-policy", "allkeys-lru"]
