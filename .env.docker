# Docker Environment Variables Template
# Copy this file to .env and set appropriate values

# Application
NODE_ENV=production
PORT=3000
DB_PATH=/app/data/frontbase.db

# Encryption - REQUIRED for persistent Supabase connections
# Generate with: node -p "require('crypto').randomBytes(32).toString('hex')"
# CRITICAL: Set this to prevent losing connections on container restart
ENCRYPTION_KEY=

# Example of a valid encryption key (DO NOT USE THIS ONE):
# ENCRYPTION_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456

# To generate your own secure key, run:
# node -p "require('crypto').randomBytes(32).toString('hex')"

# Optional: Supabase Auto-Configuration
# If all three are provided, Supabase will be automatically configured on startup
# SUPABASE_PROJECT_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_anon_key_here
# SUPABASE_SERVICE_KEY=your_service_key_here